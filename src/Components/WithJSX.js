/* we don't use React keyword any where in the code but we still import the React library 
because it transates the JSX to HTML */

import React from "react";

const WithJSX = () => {
  return <h3>'Code with JSX'</h3>;
};
export default WithJSX;
